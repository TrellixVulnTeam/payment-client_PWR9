import React from 'react';
import { FormControl, FormControlLabel, FormHelperText, makeStyles, } from '@material-ui/core';
import MyHelperText from '../../helper/FormHelperText';
import { useFormContext } from 'react-hook-form';
import { CustomCheckbox } from './CustomCheckbox';
export var FormCheckBox = function (props) {
    var _a;
    var name = props.name, label = props.label, value = props.value, onChange = props.onChange, onSync = props.onSync;
    var errors = useFormContext().formState.errors;
    var classes = useStyles();
    var handleChange = function (event) {
        onChange(event);
        onSync();
    };
    return (React.createElement(FormControl, { classes: { root: classes.MuiFormControlRoot } },
        React.createElement(FormControlLabel, { label: label, control: React.createElement(CustomCheckbox, { checked: value, indeterminate: value === undefined, onChange: handleChange }) }),
        React.createElement(FormHelperText, null,
            React.createElement(MyHelperText, { message: (_a = errors[name]) === null || _a === void 0 ? void 0 : _a.message }))));
};
var useStyles = makeStyles({
    MuiFormControlRoot: {
        marginTop: 0,
    },
});
