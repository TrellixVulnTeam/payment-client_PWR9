import React from 'react';
import { FormControl, FormControlLabel, FormHelperText, FormLabel, Radio, RadioGroup, } from '@material-ui/core';
import { useFormContext } from 'react-hook-form';
import MyHelperText from '../../helper/FormHelperText';
export var FormRadio = React.forwardRef(function (props, ref) {
    var _a;
    var errors = useFormContext().formState.errors;
    var name = props.name, label = props.label, _b = props.value, value = _b === void 0 ? '' : _b, options = props.options, 
    // helperText,
    disabled = props.disabled, onChange = props.onChange, onSync = props.onSync, _c = props.customWidth, customWidth = _c === void 0 ? false : _c, _d = props.colNum, colNum = _d === void 0 ? 1 : _d;
    var handleChange = function (event) {
        onChange(event);
        onSync();
    };
    return (React.createElement(FormControl, { component: "div", fullWidth: true, error: !!errors[name], disabled: disabled },
        React.createElement(FormLabel, null, label),
        React.createElement(RadioGroup, { row: true, "aria-label": name, name: name, value: "" + value, onChange: handleChange, ref: ref }, options === null || options === void 0 ? void 0 : options.map(function (option, index) {
            return !customWidth ? (React.createElement(FormControlLabel, { value: "" + option.value, control: React.createElement(Radio, { color: "primary" }), label: option.label, key: index })) : (React.createElement(FormControlLabel, { value: "" + option.value, control: React.createElement(Radio, { color: "primary" }), label: option.label, key: index, style: {
                    maxWidth: "calc(" + 100 / colNum + "%)",
                    flex: "0 0 calc(" + 100 / colNum + "% )",
                } }));
        })),
        React.createElement(FormHelperText, null,
            React.createElement(MyHelperText, { message: (_a = errors[name]) === null || _a === void 0 ? void 0 : _a.message }))));
});
