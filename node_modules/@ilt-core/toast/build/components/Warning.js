import React from 'react';
import { ToastType } from '../types';
import styles from '../styles/styles.module.scss';
import withLayout from '../hocs/withLayout';
var Warning = function (_a) {
    var title = _a.title, msg = _a.msg;
    return (React.createElement(React.Fragment, null,
        React.createElement("div", { className: "warning-text ".concat(styles.title) }, title),
        msg && React.createElement("div", { className: styles.message }, msg)));
};
export default withLayout(Warning, ToastType.WARNING);
