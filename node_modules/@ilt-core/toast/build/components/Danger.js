import React from 'react';
import { ToastType } from '../types';
import styles from '../styles/styles.module.scss';
import withLayout from '../hocs/withLayout';
var Danger = function (_a) {
    var title = _a.title, msg = _a.msg;
    return (React.createElement(React.Fragment, null,
        React.createElement("div", { className: "error-text ".concat(styles.title) }, title),
        msg && React.createElement("div", { className: styles.message }, msg)));
};
export default withLayout(Danger, ToastType.ERROR);
