import React from 'react';
import { ToastType } from '../types';
import styles from '../styles/styles.module.scss';
import withLayout from '../hocs/withLayout';
var Success = function (_a) {
    var title = _a.title, msg = _a.msg;
    return (React.createElement(React.Fragment, null,
        React.createElement("div", { className: "success-text ".concat(styles.title) }, title),
        msg && React.createElement("div", { className: styles.message }, msg)));
};
export default withLayout(Success, ToastType.SUCCESS);
