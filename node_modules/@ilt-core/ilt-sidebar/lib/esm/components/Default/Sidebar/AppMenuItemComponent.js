var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import React, { forwardRef } from 'react';
import ListItem from '@material-ui/core/ListItem';
import { NavLink } from 'react-router-dom';
var AppMenuItemComponent = function (props) {
    var className = props.className, onClick = props.onClick, link = props.link, children = props.children, isActive = props.isActive;
    if (!link || typeof link !== 'string') {
        return (React.createElement(ListItem, { button: true, className: className, children: children, onClick: onClick, component: 'a' }));
    }
    return (React.createElement(ListItem, { button: true, className: className + " " + (isActive ? 'active' : ''), children: children, component: forwardRef(function (props, ref) { return (React.createElement(NavLink, __assign({ exact: true }, props, { innerRef: ref }))); }), to: link }));
};
export default AppMenuItemComponent;
