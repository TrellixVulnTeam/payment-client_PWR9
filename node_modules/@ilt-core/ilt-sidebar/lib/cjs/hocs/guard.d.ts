export interface GuardProps {
    guards?: [];
}
export interface GuardParams {
    route: any;
    next: (redirectTo?: string) => void;
}
export declare class Guard {
    guards: never[];
    route: null;
    constructor(guards: any, route: any);
    runGuard: any;
    resolveAllGuards: () => Promise<{
        redirectTo: string;
    }>;
}
