export enum ButtonVariants {
  primary = 'primary',
  secondary = 'secondary',
  danger = 'danger',
  ghost = 'ghost',
  invert = 'invert',
  select = 'select',
}

export enum ButtonSizes {
  lg = 'lg',
  md = 'md',
  sm = 'sm',
}
