export enum DividerType {
  line = 'line',
  filled = 'filled',
}

export enum DividerOrientation {
  horizontal = 'horizontal',
  vertical = 'vertical',
}

export enum DividerVarients {
  fullWidth = 'fullWidth',
  inset = 'inset',
  middle = 'middle',
}
