# ALOPAY - monorepo - FRONTEND

## Frontend CLI
```
Usage:
    ./fe COMMAND APP [OPTION]
    ./fe -h

Commands:
 start      Start a web app in development mode
 build      Create a bundle for web app
```

```
For specific a env:
    ENV=beta ./fe COMMAND APP [OPTION]
```